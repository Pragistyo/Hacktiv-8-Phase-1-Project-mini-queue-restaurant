<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Index Customer</title>
  </head>
  <body>
    <div>
        <% data.forEach(meja =>{%>
            <div><%=meja.tableName %></div>
            <div>
                <a href="/customer/orders/<%=meja.id%>"><button>Orders</button></a>
                <a href="/customer/vieworder/<%=meja.id%>"><button>View Orders</button></a>
            </div>
        <%})%>
    </div>
    <div>
      <form action="/customer/waiter" method="GET">
        <fieldset >
            <select name="WaiterId" id="">
              <%dataWaiter.forEach(z=>{%>
                <option value="<%=z.id%>"><%=z.nama%></option>
            <%})%>
          </select>
         <button type="submit">View Orders</button></a>
        </fieldset>
      </form>
  </div>
  </body>
</html>